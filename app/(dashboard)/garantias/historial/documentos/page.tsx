import Link from "next/link";
import { requirePermission } from "@/lib/auth/helpers";
import { listWarrantyDocuments } from "@/modules/garantias/actions/warranty";
export default async function WarrantyDocumentsPage() { await requirePermission("warranties.documents"); const result=await listWarrantyDocuments(); if(!result.success) throw new Error(result.error); const docs=result.data as any[]; return <div className="mx-auto max-w-[1000px] space-y-5"><div><h1 className="text-2xl font-bold text-[#101828]">Historial de documentos</h1><p className="mt-1 text-sm text-[#667085]">Recibos y conduces generados por los flujos de garantía.</p></div><div className="enterprise-panel divide-y divide-[#f0f1f3]">{docs.map((doc)=><Link key={doc.id} href={`/garantias/documentos/${doc.documentCode}`} className="flex items-center justify-between gap-3 p-4 hover:bg-[#f8fafc]"><div><p className="font-mono text-sm font-bold text-indigo-600">{doc.documentCode}</p><p className="mt-1 text-sm text-[#344054]">{doc.counterpartyName} · {doc.items.length} caso(s)</p></div><span className="text-xs text-[#667085]">{doc.type.replaceAll("_", " ")}</span></Link>)}{docs.length===0&&<p className="p-8 text-center text-sm text-[#667085]">No hay documentos.</p>}</div></div>; }
