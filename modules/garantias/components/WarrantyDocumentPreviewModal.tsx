"use client";

import { useEffect, useRef } from "react";
import { Building2, Calendar, FileText, Printer, ShieldCheck, X } from "lucide-react";
import { formatDateRD } from "@/lib/utils/format";

export type WarrantyDocument = { documentCode: string; type: string; documentDate: string | Date; counterpartyName: string; notes?: string | null; items: Array<{ id: string; case: { caseCode: string; imei: string; model: string; clientName: string; problem: string } }> };
const typeLabel: Record<string, string> = { INTAKE_RECEIPT: "RECIBO DE INGRESO", TECHNICIAN_ASSIGNMENT: "ENTREGA A TÉCNICO", TECHNICIAN_RECEIPT_REPAIRED: "RECEPCIÓN DE TÉCNICO · REPARADO", TECHNICIAN_RECEIPT_UNREPAIRED: "RECEPCIÓN DE TÉCNICO · NO REPARADO", SUPPLIER_SHIPMENT: "ENVÍO A SUPLIDOR", SUPPLIER_RECEIPT: "RECEPCIÓN DE SUPLIDOR", CUSTOMER_DELIVERY: "ENTREGA AL CLIENTE" };

export function WarrantyDocumentPreviewModal({ document: doc, onClose }: { document: WarrantyDocument; onClose: () => void }) {
  const printRef = useRef<HTMLDivElement>(null);
  useEffect(() => { window.document.body.style.overflow = "hidden"; return () => { window.document.body.style.overflow = ""; }; }, []);
  function printDocument() { const node = printRef.current?.querySelector(".warranty-print-document"); if (!(node instanceof HTMLElement)) return; window.document.getElementById("warranty-print-root")?.remove(); const root = window.document.createElement("div"); root.id = "warranty-print-root"; root.innerHTML = node.outerHTML; root.querySelectorAll("[class]").forEach((element) => element.setAttribute("class", Array.from(element.classList).filter((name) => !name.startsWith("print:")).join(" "))); window.document.body.appendChild(root); window.document.body.classList.add("warranty-printing"); const cleanup = () => { window.document.body.classList.remove("warranty-printing"); root.remove(); }; window.addEventListener("afterprint", cleanup, { once: true }); window.print(); }
  const label = typeLabel[doc.type] ?? doc.type.replaceAll("_", " ");
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-3 backdrop-blur-sm sm:p-6" role="dialog" aria-modal="true"><div className="flex max-h-[96vh] w-full max-w-5xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between bg-[#991b1b] px-5 py-3 text-white sm:px-6"><div className="flex min-w-0 items-center gap-3"><FileText size={19} /><div className="min-w-0"><p className="truncate text-sm font-black">{label}</p><p className="font-mono text-[11px] text-red-100">{doc.documentCode}</p></div></div><div className="flex items-center gap-2"><button type="button" onClick={printDocument} className="inline-flex items-center gap-2 rounded-lg bg-white px-3 py-2 text-xs font-black text-red-800"><Printer size={15} /> Imprimir / PDF</button><button type="button" onClick={onClose} className="rounded-lg p-2 text-red-100 hover:bg-red-800" aria-label="Cerrar"><X size={19} /></button></div></div><div ref={printRef} className="flex-1 overflow-y-auto bg-slate-100 p-4 sm:p-8"><article className="w-full max-w-3xl space-y-6 bg-white p-6 text-slate-900 shadow-sm sm:mx-auto sm:p-8 warranty-print-document print:shadow-none print:p-0"><header className="flex items-start justify-between gap-5 border-b-2 border-red-700 pb-5"><div><div className="flex items-center gap-2"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-red-700 text-xl font-black text-white">S</div><div><h1 className="text-xl font-black tracking-tight">Señal Digital</h1><p className="text-[10px] font-bold uppercase tracking-wider text-red-700">Garantías y servicio técnico</p></div></div><p className="mt-3 text-xs text-slate-500"><Building2 className="mr-1 inline h-3.5 w-3.5 text-red-600" />La Romana, República Dominicana</p></div><div className="text-right"><span className="inline-block rounded-lg border border-red-200 bg-red-50 px-3 py-1 text-[11px] font-black uppercase tracking-wider text-red-800">{label}</span><p className="mt-2 font-mono text-lg font-black text-red-700">{doc.documentCode}</p><p className="mt-1 text-[11px] text-slate-500"><Calendar className="mr-1 inline h-3 w-3" />{formatDateRD(doc.documentDate)}</p></div></header><div className="grid gap-3 rounded-xl border border-red-100 bg-red-50/60 p-4 text-xs sm:grid-cols-2"><div><p className="text-[10px] font-bold uppercase tracking-wider text-red-700">Contraparte</p><p className="mt-1 text-sm font-bold text-slate-900">{doc.counterpartyName || "Garantía interna"}</p></div><div><p className="text-[10px] font-bold uppercase tracking-wider text-red-700">Equipos incluidos</p><p className="mt-1 text-sm font-bold text-slate-900">{doc.items.length}</p></div></div><div className="overflow-hidden rounded-xl border border-red-200"><table className="w-full text-left text-xs"><thead className="bg-red-700 text-white"><tr><th className="px-3 py-3">#</th><th className="px-3 py-3">Equipo / caso</th><th className="px-3 py-3">IMEI</th><th className="px-3 py-3">Problema</th></tr></thead><tbody className="divide-y divide-red-100">{doc.items.map((entry, index) => <tr key={entry.id}><td className="px-3 py-3 font-mono font-bold text-red-700">{index + 1}</td><td className="px-3 py-3"><strong className="block text-slate-900">{entry.case.model}</strong><span className="font-mono text-[10px] text-red-700">{entry.case.caseCode} · {entry.case.clientName}</span></td><td className="px-3 py-3 font-mono text-[10px] text-slate-600">{entry.case.imei}</td><td className="px-3 py-3 text-slate-700">{entry.case.problem}</td></tr>)}</tbody></table></div>{doc.notes && <div className="rounded-xl border border-red-200 bg-red-50 p-4 text-xs text-red-900"><strong>Observación:</strong> {doc.notes}</div>}<div className="flex items-start gap-2 rounded-xl border border-red-200 bg-red-50 p-4 text-xs text-red-900"><ShieldCheck size={17} className="mt-0.5 shrink-0 text-red-700" /><p><strong>Condiciones de garantía:</strong> la garantía puede quedar anulada si el equipo presenta daños físicos, humedad o manipulación no autorizada. Este documento deja constancia de la entrega y recepción del equipo.</p></div><div className="grid grid-cols-2 gap-10 border-t border-red-100 pt-12 text-center text-xs text-slate-600"><div><div className="border-t border-slate-400 pt-2">Entrega / responsable</div><p className="mt-1 text-[10px] text-slate-400">Firma y cédula</p></div><div><div className="border-t border-slate-400 pt-2">Recibido conforme</div><p className="mt-1 text-[10px] text-slate-400">Firma y fecha</p></div></div></article></div></div></div>;
}
